
<div class="card">
<div class="card-header">
<h1 style="color: #0C8DF6">THÔNG TIN CÁ NHÂN</h1>
</div>
<div class="card-body">
 <div class="row">
   <div class="col md-4">
     <H4> Thông tin cơ bản</H4>
  <strong>Tên: </strong> <%= current_user.employee.name %><br/>
  <strong>Tuổi: </strong> <%= current_user.employee.age %><br/>
  <strong>Ngày sinh: </strong> <%= current_user.employee.birthday %><br/>
  <strong>Địa chỉ: </strong> <%= current_user.employee.address %><br/>
   </div>
   <div class="col md-4">
     <H4> Thông tin tài khoản </H4>
  <strong>Email: </strong> <%= current_user.email %><br/>
     </div>
     <div class="col md-4">
       <H4> Thông tin bổ sung </H4>
  <% if current_user.employee.employee_file.present? %>
    <strong>Vị Trí </strong> <%= current_user.employee.employee_file.position %><br/>
  <% else %>
  <% end %>

  <strong>Role: </strong> <%= current_user.employee.role_id %><br/>
  <strong>Ngày vào công ty: </strong> <%= current_user.employee.created_at.strftime("%d/%m/%Y") %><br/>
 </div>
</div>
</div>
</div>
